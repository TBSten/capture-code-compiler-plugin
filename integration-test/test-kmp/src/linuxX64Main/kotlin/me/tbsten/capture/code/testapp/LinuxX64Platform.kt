package me.tbsten.capture.code.testapp

/**
 * linuxX64Main source set placeholder.
 *
 * task-019 (KMP sample project skeleton) で配置。各 target 別の use site / actual
 * 宣言 / hierarchy 検証は後続 task (task-020 ~ task-024) で追加される。
 */
internal const val LINUX_X64_MAIN_MARKER: String = "linuxX64"
