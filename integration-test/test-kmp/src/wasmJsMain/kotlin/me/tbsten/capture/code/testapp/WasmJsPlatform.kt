package me.tbsten.capture.code.testapp

/**
 * wasmJsMain source set placeholder.
 *
 * task-019 (KMP sample project skeleton) で配置。本 ticket と同時に `:annotation`
 * 側にも wasmJs target が追加され、`me.tbsten.capture.code` 配下の runtime API
 * も wasmJs 上で利用可能になっている。各 target 別の use site / actual 宣言 /
 * hierarchy 検証は後続 task (task-020 ~ task-024) で追加される。
 */
internal const val WASM_JS_MAIN_MARKER: String = "wasmJs"
