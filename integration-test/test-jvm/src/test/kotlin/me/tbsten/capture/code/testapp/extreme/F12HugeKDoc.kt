package me.tbsten.capture.code.testapp.extreme

import me.tbsten.capture.code.CaptureCode
import me.tbsten.capture.code.Source

// ============================================================================
// F12 — KDoc 巨大 (100 行)
//
// extractKdocPrefix が 100 行 KDoc を全行抽出できるか、 dedent が正しく機能するかを観察。
// ============================================================================

@CaptureCode
@Target(AnnotationTarget.FUNCTION)
@Retention(AnnotationRetention.SOURCE)
internal annotation class Probe_F12_HugeKDoc(val source: Source = Source())

/**
 * KDoc line 0 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 1 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 2 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 3 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 4 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 5 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 6 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 7 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 8 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 9 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 10 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 11 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 12 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 13 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 14 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 15 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 16 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 17 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 18 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 19 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 20 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 21 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 22 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 23 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 24 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 25 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 26 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 27 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 28 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 29 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 30 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 31 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 32 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 33 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 34 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 35 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 36 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 37 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 38 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 39 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 40 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 41 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 42 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 43 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 44 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 45 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 46 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 47 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 48 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 49 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 50 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 51 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 52 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 53 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 54 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 55 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 56 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 57 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 58 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 59 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 60 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 61 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 62 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 63 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 64 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 65 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 66 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 67 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 68 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 69 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 70 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 71 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 72 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 73 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 74 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 75 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 76 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 77 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 78 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 79 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 80 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 81 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 82 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 83 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 84 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 85 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 86 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 87 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 88 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 89 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 90 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 91 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 92 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 93 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 94 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 95 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 96 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 97 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 98 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 * KDoc line 99 — describing extreme input partition F12 with a long-form note that exercises the KDoc extraction logic against a 100-line documentation block.
 */
@Probe_F12_HugeKDoc
fun hugeKDocFunction(): Int = 42
